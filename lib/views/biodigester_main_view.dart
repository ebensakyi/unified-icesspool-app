import 'package:flutter/material.dart';

class BiodigesterMainView extends StatelessWidget {
  const BiodigesterMainView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Bio"),
    );
  }
}
